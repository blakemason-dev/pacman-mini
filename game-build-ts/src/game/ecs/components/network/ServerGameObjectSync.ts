import {
    defineComponent,
    Types,
} from 'bitecs';

export const ServerGameObjectSync = defineComponent({
    serverEid: Types.ui8,
    // interpolation: Types.ui8,
    // needsUpdate: Types.ui8
});